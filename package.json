{
  "name": "bizbooks-app",
  "version": "1.0.0",
  "description": "BizBooks - Small Business Accounting App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static app - no build needed'",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build-apk": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}